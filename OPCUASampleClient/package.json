{
    "name": "node-red-test",
    "description": "testing node-red git functionalities",
    "version": "0.0.1",
    "dependencies": {
        "node-red": "latest",
        "node-red-contrib-opcua": "0.2.89"
    },
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json",
            "settings": "settings.js"
        }
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node-red -p 1880 -D httpAdminRoot='/admin' data/flows.json"
    }
}